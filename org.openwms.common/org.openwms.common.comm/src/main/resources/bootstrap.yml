spring:
  application:
    name: tcpip-driver
  main:
    banner-mode: "off"

server:
  port: 8084

eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/
